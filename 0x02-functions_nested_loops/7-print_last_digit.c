#include "main.h"

/**
 * print_last_digit - function that prints the last digit of a number.
 * @n: number to check
 * @c: last digit of number n
 * Return: 0
 */
int print_last_digit(int n)
{
	int c = n % 10;
	_putchar(c);
	return (0);
}
